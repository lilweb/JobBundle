{
    "id" : {{ job.id }},
    "name" : "{{ job.name }}",
    "dateCreation" : {{ job.creationDate | date('U') }},
    "status" : {{ job.lastStatus }},
    "parameters": {
        {% for column in columns %}
            "{{ column }}" : "{{ job.getParameter(column) }}"{% if not loop.last %},{% endif %}
        {% endfor %}
    }
}