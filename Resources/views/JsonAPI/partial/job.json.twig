{
    "id" : {{ job.id }},
    "name" : "{{ job.name }}",
    "dateCreation" : {{ job.creationDate | date('U') }},
    "startedBy" : "{{ job.jobRunner }}",
    "lastStatus" : {{ job.lastStatus }},
    "globalStatus": "{{ job.getGlobalStatus }}",
    "parameters": {
        {% for column in columns %}
            "{{ column }}" : "{{ job.getParameter(column) }}"{% if not loop.last %},{% endif %}
        {% endfor %}
    }
}