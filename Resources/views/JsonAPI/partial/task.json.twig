{
    "id" : {{ task.id }},
    "name" : "{{ task.name }}",
    "dateCreation" : {{ task.creationDate | date('U') }},
    "dateUpdate" : {{ task.lastStatusUpdateDate | date('U') }},
    "status" : "{{ task.status }}",
    "message" : "{{ task.infoMsg }}"
}