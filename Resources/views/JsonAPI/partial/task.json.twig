{
    "id" : {{ task.id }},
    "name" : "{{ task.name }}",
    "dateCreation" : {{ task.executionDate | date('U') }},
    "dateUpdate" : {{ task.lastStatusUpdateDate | date('U') }},
    "status" : {{ task.status }},
    "message": "{{ task.infoMsg }}"
}