{
    "id" : {{ task.id }},
    "name" : "{{ task.name }}",
    "dateExecution" : {{ task.executionDate | date('U') }},
    "dateUpdate" : {{ task.lastStatusUpdateDate | date('U') }},
    "status" : "{{ task.status }}",
    "message" : "{{ task.infoMsg | replace({'\r\n': '', '\n': ''}) }}"
}